# sakhiba_Nestack_Submission
Built a PDF vectorization pipeline using LangChain that loads, splits, and embeds documents, storing them in a Chroma vector database. Developed a FastAPI endpoint to perform similarity search and return top-k relevant text chunks with scores, without using any LLM or retrieval chains.
score: vector similarity-search score/distance returned by Chroma
